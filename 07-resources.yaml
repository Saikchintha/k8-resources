apiVersion: v1
kind: Pod
metadata:
  name: env-demo
  
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    resources:
     requests:
      cpu: "100m"
      memory: "128mi"
    limits:
      cpu: "250m"
      memory: "256mi"